export { CalendarView } from './CalendarView';

/** Module init — no-op, subscriptions handled inside CalendarView. */
export function init(): void {
  console.log('[module:calendar] initialised');
}
